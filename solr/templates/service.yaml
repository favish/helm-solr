apiVersion: v1
kind: Service
metadata:
  name: {{ template "solr.fullname" . }}
  labels:
    app: {{ template "solr.fullname" . }}
spec:
  ports:
  - port: 8983
    name: solr
  clusterIP: None
  selector:
    app: {{ template "solr.fullname" . }}
